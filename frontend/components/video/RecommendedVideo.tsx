import Link from "next/link";import {Video} from "@/types";import {VideoThumbnail} from "./VideoThumbnail";
export function RecommendedVideo({video}:{video:Video}){return <Link href={`/watch/${video.id}`} className="group flex gap-2"><div className="relative aspect-video w-40 shrink-0 overflow-hidden rounded-lg bg-surface"><VideoThumbnail thumbnailUrl={video.thumbnailUrl} videoSrc={video.videoSrc} alt={video.title} priority className="object-cover transition group-hover:scale-105"/><span className="absolute bottom-1 right-1 rounded bg-black/85 px-1 py-0.5 text-[10px] font-semibold">{video.duration}</span></div><div className="min-w-0"><h3 className="line-clamp-2 text-sm font-semibold leading-5">{video.title}</h3><p className="mt-1 text-xs text-muted-foreground">{video.creator.name}</p><p className="mt-0.5 text-xs text-muted-foreground">{video.views} • {video.uploadedAt}</p></div></Link>}
