import {BarChart3,Loader2,Search} from "lucide-react";
import {SelectedCampaign} from "@/types";
export function CampaignRecommendation({campaign,loading,error,onFind,onPreview,previewLoading}:{campaign?:SelectedCampaign;loading:boolean;error?:string;onFind:()=>void;onPreview:()=>void;previewLoading:boolean}){if(loading)return <section className="mt-5 rounded-xl border border-border bg-surface p-5"><p className="font-medium">Finding the best campaign…</p><p className="mt-1 text-sm text-muted-foreground">Querying campaign intelligence and matching historical performance.</p><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-elevated"><div className="animate-sweep h-full w-1/4 rounded-full bg-adless-cyan"/></div></section>;if(!campaign)return <div className="mt-5 border-t border-border pt-5"><button onClick={onFind} className="inline-flex h-11 items-center gap-2 rounded-xl bg-adless-cyan px-6 text-sm font-semibold text-black"><Search className="h-4 w-4"/>Find Best Campaign</button>{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}</div>;return <section className="mt-5 rounded-xl border border-border bg-surface p-5"><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Recommended campaign</p><div className="mt-3 flex flex-wrap items-center gap-4"><span className="grid h-12 w-12 place-items-center rounded-xl bg-adless-cyan/10 font-display text-lg font-bold text-adless-cyan">{campaign.brand.split(" ").map(w=>w[0]).join("").slice(0,2)}</span><div><h4 className="font-display text-xl font-semibold">{campaign.brand}</h4><p className="text-sm text-muted-foreground">{campaign.productName}</p></div><div className="ml-auto flex gap-8 text-sm"><Metric label="Category" value={campaign.category}/><Metric label="Market" value={campaign.market}/></div></div><div className="mt-5 grid grid-cols-2 gap-3 lg:grid-cols-4"><Box label="Performance" value={`${campaign.performanceScore} / 10`}/><Box label="Success Rate" value={`${Math.round(campaign.successRate*100)}%`}/><Box label="Average Exposure" value={`${campaign.avgExposureSeconds.toFixed(1)} sec`}/><Box label="AI Confidence" value={`${Math.round(campaign.selectionConfidence*100)}%`}/></div><p className="mt-4 text-sm leading-6 text-muted-foreground"><span className="font-medium text-white">Reason: </span>{campaign.reason}</p><p className="mt-3 flex items-center gap-2 text-xs text-muted-foreground"><BarChart3 className="h-3.5 w-3.5"/>Campaign intelligence powered by ClickHouse</p><div className="mt-5 flex gap-3"><button onClick={onPreview} disabled={previewLoading} className="inline-flex h-10 items-center gap-2 rounded-xl bg-adless-cyan px-6 text-sm font-semibold text-black disabled:opacity-60">{previewLoading&&<Loader2 className="h-4 w-4 animate-spin"/>}{previewLoading?"Rendering preview…":"Preview Placement"}</button><button onClick={onFind} className="h-10 rounded-xl bg-elevated px-5 text-sm font-medium">Choose Another</button></div></section>}
function Metric({label,value}:{label:string;value:string}){return <div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 font-medium capitalize">{value}</p></div>};function Box({label,value}:{label:string;value:string}){return <div className="rounded-lg border border-border bg-background p-3"><p className="text-xs text-muted-foreground">{label}</p><p className="font-display mt-1 text-lg font-semibold">{value}</p></div>}
