import {LucideIcon} from "lucide-react";
export function StudioMetric({label,value,delta,icon:Icon,accent=false}:{label:string;value:string;delta:string;icon?:LucideIcon;accent?:boolean}){return <div className="rounded-xl border border-border bg-surface p-4"><div className="flex items-center justify-between"><p className="text-xs font-medium uppercase tracking-wide text-muted-foreground">{label}</p>{Icon&&<Icon className={`h-4 w-4 ${accent?"text-adless-cyan":"text-muted-foreground"}`}/>}</div><p className={`font-display mt-3 text-2xl font-semibold ${accent?"text-adless-cyan":""}`}>{value}</p><p className="mt-1 text-xs text-muted-foreground">{delta}</p></div>}
